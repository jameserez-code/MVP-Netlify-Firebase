export const swrDashboardConfig = {
  refreshInterval: 10000,
  revalidateOnFocus: true,
  errorRetryCount: 3,
  dedupingInterval: 2000,
}

export const swrFastConfig = {
  refreshInterval: 5000,
  revalidateOnFocus: true,
  errorRetryCount: 2,
}

export const swrActivityConfig = {
  refreshInterval: 5000,
  revalidateOnFocus: true,
  errorRetryCount: 3,
  dedupingInterval: 2000,
}
